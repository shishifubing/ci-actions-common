name: release

on:
  schedule:
    # daily at 23:00 UTC
    - cron: 0 23 * * *
  workflow_dispatch:
  pull_request:

jobs:
  release:
    uses: ./.github/workflows/release.yml
    secrets: inherit
    with:
      draft: true
